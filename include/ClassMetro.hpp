#include <iostream>
using namespace std;
class Metro
{
private:
    /* data */
    
public:
    static const int MoneyCost = 3267; //cost in money per line
    static const int TimeCost = 1; //cost in time per kilometer
    static int HourTime;
    Metro(/* args */);
    ~Metro();
};