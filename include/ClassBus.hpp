#include <iostream>
using namespace std;
class Bus
{
private:
    /* data */
    
public:
    static const int MoneyCost = 2250; //cost in money per line
    static const int TimeCost = 4; //cost in time per kilometer
    static int HourTime;
    Bus(/* args */);
    ~Bus();
};